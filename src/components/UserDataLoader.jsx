import React from "react";

const UserDataLoader = () => {
  return (
    <div className=" fixed inset-0 bg-black bg-opacity-30 z-10 flex items-center justify-center">
      <div className="spinner"></div>
    </div>
  );
};

export default UserDataLoader;
